"use client";

import Link from "next/link";
import { useParams, useSearchParams } from "next/navigation";
import { useEffect, useState } from "react";

type Task = { id: string; description: string; assignee: "OWN TASK" | "OWNER"; creator: string; dueDate: string; dueTime: string; highPriority: boolean; createdAt: string; forwardedToOwner?: boolean };
const accents: Record<string, string> = { paradise: "#d6a65e", respawn: "#28d7c3", horizon: "#f97316", trident: "#38bdf8" };

export default function ManagerTasksPage() {
  const { agency, manager } = useParams<{ agency: string; manager: string }>(); const label = useSearchParams().get("label") || "MANAGER"; const accent = accents[agency] || "#facc15";
  const [tasks, setTasks] = useState<Task[]>([]); const [description, setDescription] = useState(""); const [assignee, setAssignee] = useState<Task["assignee"]>("OWN TASK"); const [dueDate, setDueDate] = useState(""); const [dueTime, setDueTime] = useState(""); const [highPriority, setHighPriority] = useState(false); const [message, setMessage] = useState("LOADING TASKS...");
  async function load() { const response = await fetch(`/api/manager-tasks?agency=${agency}&manager=${encodeURIComponent(manager)}`, { cache: "no-store" }); const data = await response.json(); setTasks(data.tasks || []); setMessage(response.ok ? "UP TO DATE." : data.error || "COULD NOT LOAD TASKS."); }
  async function save(next: Task[]) { setTasks(next); const response = await fetch("/api/manager-tasks", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ agency, manager, managerLabel: label, tasks: next }) }); const data = await response.json(); if (response.ok) setTasks(data.tasks || next); setMessage(response.ok ? "TASKS SAVED." : data.error || "COULD NOT SAVE TASKS."); }
  useEffect(() => { void load(); }, [agency, manager]);
  function add() { if (!description.trim()) return setMessage("ADD WHAT NEEDS DOING FIRST."); const task: Task = { id: crypto.randomUUID(), description: description.trim().toUpperCase(), assignee, creator: "", dueDate, dueTime, highPriority, createdAt: new Date().toISOString() }; setDescription(""); setDueDate(""); setDueTime(""); setHighPriority(false); void save([task, ...tasks]); }
  return <main className="min-h-screen bg-[#080806] px-5 py-8 text-white sm:px-8"><div className="mx-auto max-w-5xl"><header className="flex justify-between"><Link href={`/agency/${agency}/managers/${manager}?label=${encodeURIComponent(label)}`} className="text-xs font-black uppercase tracking-widest" style={{ color: accent }}>← BACK TO MANAGER PORTAL</Link><button onClick={() => void load()} className="text-xs font-black uppercase" style={{ color: accent }}>REFRESH</button></header><p className="mt-10 text-xs font-black uppercase tracking-[.3em]" style={{ color: accent }}>{label}</p><h1 className="mt-4 font-[family-name:var(--font-norwester)] text-5xl uppercase">TASK SPACE</h1><section className="mt-8 rounded-3xl border bg-white/[.04] p-5" style={{ borderColor: `${accent}55` }}><div className="grid gap-3 md:grid-cols-2"><input value={description} onChange={(event) => setDescription(event.target.value)} placeholder="WHAT NEEDS DOING?" className="rounded-xl border border-white/15 bg-black/40 px-4 py-3 text-lg font-bold uppercase text-white"/><select value={assignee} onChange={(event) => setAssignee(event.target.value as Task["assignee"])} className="rounded-xl border border-white/15 bg-black/40 px-4 py-3 text-lg font-bold text-white"><option value="OWN TASK">OWN TASK</option><option value="OWNER">OWNER</option></select><div className="flex gap-3"><input type="date" value={dueDate} onChange={(event) => setDueDate(event.target.value)} className="min-w-0 flex-1 rounded-xl border bg-black/40 px-4 py-3 text-lg font-bold text-white" style={{ borderColor: `${accent}88` }}/><input type="time" value={dueTime} onChange={(event) => setDueTime(event.target.value)} className="min-w-0 flex-1 rounded-xl border bg-black/40 px-4 py-3 text-lg font-bold text-white" style={{ borderColor: `${accent}88` }}/></div></div><div className="mt-3 flex items-center justify-between"><label className="text-sm font-bold text-red-200"><input type="checkbox" checked={highPriority} onChange={(event) => setHighPriority(event.target.checked)}/> HIGH PRIORITY</label><button onClick={add} className="rounded-xl px-5 py-3 text-xs font-black uppercase text-black" style={{ background: accent }}>ADD TASK</button></div><p className="mt-3 text-sm font-bold" style={{ color: accent }}>{message}</p><div className="mt-6 space-y-3">{tasks.map((task) => <article key={task.id} className="flex flex-wrap items-center gap-4 rounded-2xl border border-white/10 bg-black/30 p-4"><input type="checkbox" aria-label="COMPLETE TASK" onChange={() => void save(tasks.filter((item) => item.id !== task.id))}/><div className="flex-1"><p className="font-black">{task.description}</p><p className="mt-2 text-sm font-bold" style={{ color: accent }}>{task.dueDate ? `DUE ${task.dueDate}${task.dueTime ? ` ${task.dueTime}` : ""}` : "NO DUE DATE"}</p></div><span className="rounded-full border px-3 py-1 text-xs font-black" style={{ borderColor: `${accent}66`, color: accent }}>{task.assignee}</span><button onClick={() => void save(tasks.filter((item) => item.id !== task.id))} className="text-xs font-black text-red-300">REMOVE</button></article>)}{!tasks.length ? <p className="rounded-2xl border border-dashed border-white/15 p-6 text-center text-sm font-bold text-white/45">NO TASKS FOR THIS MANAGER YET.</p> : null}</div></section></div></main>;
}
