import { NextResponse } from "next/server";
import { submissionsSupabase } from "@/lib/submissions-supabase";

export const dynamic = "force-dynamic";
type Row = Record<string, unknown>;
type Recruit = { id: string; username: string; joined: string; managerKey: string; manager: string; group: string; diamonds: number; hours: number; dph: number };
const SETTINGS_NAME = "manager-assignment-settings";
const text = (value: unknown) => String(value || "").trim();
const number = (value: unknown) => Number(text(value).replace(/[^\d.-]/g, "")) || 0;
const key = (value: unknown) => text(value).toLowerCase().replace(/[^a-z0-9]/g, "");
const managerIdentity = (value: unknown) => key(value).replace(/(outlook|gmail|mail)com$/, "");
const isExcludedManager = (value: unknown) => managerIdentity(value) === "mikehalesjb";
const managerRaw = (row: Row) => text(row.manager_email || row.creator_network_manager || row["Creator Network manager"] || row.email);
const creatorId = (row: Row) => { const id = text(row.creator_id || row["Creator ID"]); return /^\d{8,}$/.test(id) ? id : key(row.creator_username || row["Creator's username"]); };
const username = (row: Row) => text(row.creator_username || row["Creator's username"] || row.username).replace(/^@/, "");
const date = (value: Date) => value.toISOString().slice(0, 10);
const joinedOn = (statDate: string, days: number) => { const value = new Date(`${statDate}T12:00:00`); value.setDate(value.getDate() - Math.max(0, days - 2)); return date(value); };
const label = (raw: string) => { const local = raw.split("@")[0].replace(/^firstclassagency[_.-]?/i, "").replace(/[_.-]+/g, " ").trim(); return local ? `Team ${local.split(" ").map((part) => part[0]?.toUpperCase() + part.slice(1)).join(" ")}` : "Unassigned"; };
async function rowsBetween(start: string, end: string) { const rows: Row[] = []; for (let from = 0, more = true; more; from += 1000) { const { data, error } = await submissionsSupabase.from("creator_daily_stats").select("*").gte("stat_date", start).lte("stat_date", end).order("stat_date", { ascending: true }).range(from, from + 999); if (error) throw new Error(error.message); const batch = (data || []) as Row[]; rows.push(...batch); more = batch.length === 1000; } return rows; }
export async function GET(request: Request) { try { const period = new URL(request.url).searchParams.get("period") || "7", maxDays = period === "14" ? 14 : period === "month" ? 31 : 7; const [{ data: latest }, { data: settings, error: settingsError }] = await Promise.all([submissionsSupabase.from("creator_daily_stats").select("stat_date").order("stat_date", { ascending: false }).limit(1).maybeSingle(), submissionsSupabase.from("poster_templates").select("template_json").eq("name", SETTINGS_NAME).maybeSingle()]); if (settingsError) throw new Error(settingsError.message); const endDate = text(latest?.stat_date); if (!endDate) return NextResponse.json({ recruits: [], managers: [], groups: [], startDate: "", endDate: "" }); const end = new Date(`${endDate}T12:00:00`), start = period === "month" ? new Date(end.getFullYear(), end.getMonth(), 1, 12) : new Date(end); if (period !== "month") start.setDate(start.getDate() - (maxDays - 1)); const startDate = date(start); const assignment = ((settings?.template_json as { assignments?: { managerGroups?: Record<string, string>; managerNames?: Record<string, string>; deletedManagers?: string[] } } | null)?.assignments || {}), groups = assignment.managerGroups || {}, names = assignment.managerNames || {}; const deleted = new Set((assignment.deletedManagers || []).map(managerIdentity)); const groupForManager = (manager: string) => Object.entries(groups).find(([savedManager]) => managerIdentity(savedManager) === managerIdentity(manager))?.[1] || "Unassigned"; const nameForManager = (manager: string) => Object.entries(names).find(([savedManager]) => managerIdentity(savedManager) === managerIdentity(manager))?.[1] || ""; const [latestRows, periodRows] = await Promise.all([rowsBetween(endDate, endDate), rowsBetween(startDate, endDate)]); const selected = new Map<string, { row: Row; days: number }>(); for (const row of latestRows) { const days = number(row.days_since_joining || row["Days since joining"]), id = creatorId(row), joined = joinedOn(endDate, days); const isInPeriod = period === "month" ? joined >= startDate && joined <= endDate : days >= 0 && days <= maxDays; if (id && isInPeriod) selected.set(id, { row, days }); } const byCreator = new Map<string, Row[]>(); for (const row of periodRows) { const id = creatorId(row); if (id && selected.has(id)) byCreator.set(id, [...(byCreator.get(id) || []), row]); } const recruits: Recruit[] = []; for (const [id, selectedCreator] of selected) { const rows = byCreator.get(id) || [selectedCreator.row], first = [...rows].sort((a, b) => text(a.stat_date).localeCompare(text(b.stat_date)))[0], managerKey = managerIdentity(managerRaw(first)); if (!managerKey || deleted.has(managerKey) || isExcludedManager(managerKey)) continue; const diamonds = rows.reduce((sum, row) => sum + number(row.diamonds || row.Diamonds), 0), hours = rows.reduce((sum, row) => sum + number(row.live_hours || row["Live hours"]), 0); recruits.push({ id, username: username(selectedCreator.row) || username(first) || id, joined: joinedOn(endDate, selectedCreator.days), managerKey, manager: nameForManager(managerKey) || label(managerRaw(first)), group: groupForManager(managerKey), diamonds, hours, dph: hours ? Math.round(diamonds / hours) : 0 }); } recruits.sort((a, b) => b.diamonds - a.diamonds || a.username.localeCompare(b.username)); const managers = new Map<string, { key: string; manager: string; group: string; recruits: number; diamonds: number; hours: number; dph: number }>(); for (const [savedManager, group] of Object.entries(groups)) { const managerKey = managerIdentity(savedManager); if (!managerKey || deleted.has(managerKey) || isExcludedManager(managerKey) || group === "Excluded") continue; managers.set(managerKey, { key: managerKey, manager: nameForManager(savedManager) || label(savedManager), group, recruits: 0, diamonds: 0, hours: 0, dph: 0 }); } for (const recruit of recruits) { const current = managers.get(recruit.managerKey) || { key: recruit.managerKey, manager: recruit.manager, group: recruit.group, recruits: 0, diamonds: 0, hours: 0, dph: 0 }; current.recruits += 1; current.diamonds += recruit.diamonds; current.hours += recruit.hours; current.dph = current.hours ? Math.round(current.diamonds / current.hours) : 0; managers.set(recruit.managerKey, current); } const managerList = [...managers.values()].sort((a, b) => b.recruits - a.recruits || a.manager.localeCompare(b.manager)); return NextResponse.json({ period, startDate, endDate, recruits, managers: managerList, groups: [...new Set(managerList.map((manager) => manager.group))].sort() }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Could not load recruitment quality." }, { status: 500 }); } }
