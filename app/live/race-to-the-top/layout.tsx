import type { Metadata } from "next";
export const metadata: Metadata = { title: "Race to the Top | First Class" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
